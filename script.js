let barang = [];

function submit() {}

function menambahkan() {
  var kodebar = parseFloat.getBarangById(kodebar, jumlah);
  var jumlah = parseFloat.getBarangById(kodebar, jumlah);

  var hasil = kodebar + jumlah;
}

if (((jumlah = ""), (kodebar = ""))) {
  alert("Kolom Tidak Boleh Kosong");
}
